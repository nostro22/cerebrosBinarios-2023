import { DiferenciaMinutosPipe } from './diferencia-minutos.pipe';

describe('DiferenciaMinutosPipe', () => {
  it('create an instance', () => {
    const pipe = new DiferenciaMinutosPipe();
    expect(pipe).toBeTruthy();
  });
});
