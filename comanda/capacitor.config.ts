import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'la.comanda.app',
  appName: 'laComanda',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
